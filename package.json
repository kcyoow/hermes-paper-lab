{
  "name": "hermes-paper-lab",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "materialize": "node scripts/materialize-papers.mjs",
    "predev": "npm run materialize",
    "dev": "astro dev --host 0.0.0.0",
    "prebuild": "npm run materialize",
    "build": "astro build",
    "preview": "astro preview --host 0.0.0.0"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.9",
    "astro": "^6.2.1",
    "typescript": "^6.0.3"
  }
}
